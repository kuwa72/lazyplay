#include "aes_cbc.h"

#include <cstring>

namespace {

// FIPS-197 AES inverse S-box
const uint8_t INV_SBOX[256] = {
    
};

const uint8_t SBOX[256] = {
    
};

inline uint32_t SubWord(uint32_t w) {
    return (SBOX[(w >> 24) & 0xFF] << 24) | (SBOX[(w >> 16) & 0xFF] << 16) |
           (SBOX[(w >> 8) & 0xFF] << 8) | SBOX[w & 0xFF];
}
inline uint32_t RotWord(uint32_t w) { return (w << 8) | (w >> 24); }

inline uint8_t XTime(uint8_t b) { return static_cast<uint8_t>((b << 1) ^ ((b & 0x80) ? 0x1B : 0)); }

inline uint8_t Mul(uint8_t a, uint8_t b) {
    uint8_t r = 0;
    while (b) {
        if (b & 1) r ^= a;
        a = XTime(a);
        b >>= 1;
    }
    return r;
}

} // namespace

void AesCbc::InitDecrypt(const uint8_t key[16], const uint8_t iv[16]) {
    static const uint8_t RCON[10] = { 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36 };
    for (int i = 0; i < 4; ++i) {
        m_roundKey[i] = (key[i * 4] << 24) | (key[i * 4 + 1] << 16) | (key[i * 4 + 2] << 8) | key[i * 4 + 3];
    }
    for (int i = 4; i < 44; ++i) {
        uint32_t temp = m_roundKey[i - 1];
        if (i % 4 == 0) temp = SubWord(RotWord(temp)) ^ (static_cast<uint32_t>(RCON[i / 4 - 1]) << 24);
        m_roundKey[i] = m_roundKey[i - 4] ^ temp;
    }
    memcpy(m_iv, iv, 16);
    m_initialized = true;
}

void AesCbc::DecryptBlock(const uint8_t in[16], uint8_t out[16]) {
    uint8_t s[16];
    memcpy(s, in, 16);

    auto addRoundKey = [&](int round) {
        for (int i = 0; i < 4; ++i) {
            uint32_t rk = m_roundKey[round * 4 + i];
            s[i * 4 + 0] ^= static_cast<uint8_t>((rk >> 24) & 0xFF);
            s[i * 4 + 1] ^= static_cast<uint8_t>((rk >> 16) & 0xFF);
            s[i * 4 + 2] ^= static_cast<uint8_t>((rk >> 8) & 0xFF);
            s[i * 4 + 3] ^= static_cast<uint8_t>(rk & 0xFF);
        }
    };
    auto invSubBytes = [&]() { for (int i = 0; i < 16; ++i) s[i] = INV_SBOX[s[i]]; };
    auto invShiftRows = [&]() {
        uint8_t t;
        t = s[13]; s[13] = s[9]; s[9] = s[5]; s[5] = s[1]; s[1] = t;          // row 1: right by 1
        t = s[2]; s[2] = s[10]; s[10] = t; t = s[6]; s[6] = s[14]; s[14] = t; // row 2: by 2
        t = s[3]; s[3] = s[7]; s[7] = s[11]; s[11] = s[15]; s[15] = t;        // row 3: left by 1
    };
    auto invMixColumns = [&]() {
        for (int c = 0; c < 4; ++c) {
            uint8_t* col = s + c * 4;
            uint8_t a0 = col[0], a1 = col[1], a2 = col[2], a3 = col[3];
            col[0] = Mul(a0, 14) ^ Mul(a1, 11) ^ Mul(a2, 13) ^ Mul(a3, 9);
            col[1] = Mul(a0, 9) ^ Mul(a1, 14) ^ Mul(a2, 11) ^ Mul(a3, 13);
            col[2] = Mul(a0, 13) ^ Mul(a1, 9) ^ Mul(a2, 14) ^ Mul(a3, 11);
            col[3] = Mul(a0, 11) ^ Mul(a1, 13) ^ Mul(a2, 9) ^ Mul(a3, 14);
        }
    };

    addRoundKey(10);
    for (int round = 9; round >= 1; --round) {
        invShiftRows();
        invSubBytes();
        addRoundKey(round);
        invMixColumns();
    }
    invShiftRows();
    invSubBytes();
    addRoundKey(0);

    memcpy(out, s, 16);
}

void AesCbc::EncryptBlockFwd(const uint8_t in[16], uint8_t out[16]) {
    uint8_t s[16];
    memcpy(s, in, 16);

    auto addRoundKey = [&](int round) {
        for (int i = 0; i < 4; ++i) {
            uint32_t rk = m_roundKey[round * 4 + i];
            s[i * 4 + 0] ^= static_cast<uint8_t>((rk >> 24) & 0xFF);
            s[i * 4 + 1] ^= static_cast<uint8_t>((rk >> 16) & 0xFF);
            s[i * 4 + 2] ^= static_cast<uint8_t>((rk >> 8) & 0xFF);
            s[i * 4 + 3] ^= static_cast<uint8_t>(rk & 0xFF);
        }
    };
    auto subBytes = [&]() { for (int i = 0; i < 16; ++i) s[i] = SBOX[s[i]]; };
    auto shiftRows = [&]() {
        uint8_t t;
        t = s[1]; s[1] = s[5]; s[5] = s[9]; s[9] = s[13]; s[13] = t;
        t = s[2]; s[2] = s[10]; s[10] = t; t = s[6]; s[6] = s[14]; s[14] = t;
        t = s[15]; s[15] = s[11]; s[11] = s[7]; s[7] = s[3]; s[3] = t;
    };
    auto mixColumns = [&]() {
        for (int c = 0; c < 4; ++c) {
            uint8_t* col = s + c * 4;
            uint8_t a0 = col[0], a1 = col[1], a2 = col[2], a3 = col[3];
            col[0] = Mul(a0, 2) ^ Mul(a1, 3) ^ a2 ^ a3;
            col[1] = a0 ^ Mul(a1, 2) ^ Mul(a2, 3) ^ a3;
            col[2] = a0 ^ a1 ^ Mul(a2, 2) ^ Mul(a3, 3);
            col[3] = Mul(a0, 3) ^ a1 ^ a2 ^ Mul(a3, 2);
        }
    };

    addRoundKey(0);
    for (int round = 1; round < 10; ++round) {
        subBytes();
        shiftRows();
        mixColumns();
        addRoundKey(round);
    }
    subBytes();
    shiftRows();
    addRoundKey(10);
    memcpy(out, s, 16);
}

void AesCbc::InitEncrypt(const uint8_t key[16], const uint8_t iv[16]) {
    InitDecrypt(key, iv); // same key schedule
}

void AesCbc::EncryptPacket(const uint8_t* in, uint8_t* out, size_t len) {
    if (!m_initialized) return;
    size_t blocks = len / 16;
    uint8_t prev[16];
    memcpy(prev, m_iv, 16);
    for (size_t b = 0; b < blocks; ++b) {
        uint8_t x[16];
        for (int i = 0; i < 16; ++i) x[i] = in[b * 16 + i] ^ prev[i];
        EncryptBlockFwd(x, out + b * 16);
        memcpy(prev, out + b * 16, 16);
    }
    size_t done = blocks * 16;
    if (done < len) memcpy(out + done, in + done, len - done);
}

void AesCbc::DecryptPacket(const uint8_t* in, uint8_t* out, size_t len) {
    if (!m_initialized) return;

    // Only complete 16-byte blocks are encrypted; the IV resets per packet
    size_t blocks = len / 16;
    uint8_t prev[16];
    memcpy(prev, m_iv, 16);
    for (size_t b = 0; b < blocks; ++b) {
        uint8_t plain[16];
        DecryptBlock(in + b * 16, plain);
        for (int i = 0; i < 16; ++i) out[b * 16 + i] = plain[i] ^ prev[i];
        memcpy(prev, in + b * 16, 16);
    }
    size_t done = blocks * 16;
    if (done < len) memcpy(out + done, in + done, len - done); // partial tail is plaintext
}
